FIRST_START = TRUE

LAT=-34.6131
LON=-58.3772

UMBRAL_BATERIA = 15
VENTANA_ACTIVA = NONE
CIERRE_FORZADO = FALSE

# Servidor de almacenamiento (Google Drive ya no se usa). Remoto rclone
# (sftp, ver config/rclone.conf.ejemplo) y carpeta destino dentro de el.
# Los scripts usan estos valores por defecto si faltan.
SYNC_REMOTE = servidor
SYNC_PATH = data

# Retencion de audio LOCAL (ver limpiar_retencion.sh, llamado desde los
# cierres SOLO si el "rclone copy" de esa misma corrida salio bien). Cuando
# el total supera el limite se borran carpetas de FECHA ENTERAS
# (By_Date/<fecha>/) empezando por la mas vieja. Es proteccion de la
# microSD: si se llena el equipo deja de grabar. Del servidor no se borra
# nada.
RETENCION_AUDIO_LOCAL_MB = 3072
